<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <form id="Trajan">
      <section>
        <input id="commentBox">
      </section>

      <section>
        <button class="btn btn-primary" id="submit">Comment</button>
      </section>

      <section id="commentsSection"> 
        
        <ul class="saved_comments">
          {{#each comments}}
            <li>{{this}}</li>
          {{/each}}
        </ul>
      </section>

      <section>
        <ul class="saved_comments">
          {{#each comments}}
            <li>{{this}}</li>
          {{/each}}
        </ul>
      </section>
    </form>
  </p>
</div>
<script src="/js/project.js"></script>